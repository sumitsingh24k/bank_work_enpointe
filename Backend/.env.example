# Copy this file to .env and replace <db_password> with your password
# Example MongoDB Atlas connection (replace <db_password> and optionally DB name)
MONGO_URI=mongodb+srv://sumit:<db_password>@cluster0.vbrgoik.mongodb.net/banking?retryWrites=true&w=majority

# JSON Web Token secret (change in production)
JWT_SECRET=123456789abcdef

# Optional: port the server should listen on
PORT=5000
